namespace Verve.MVC
{
    /// <summary>
    /// MVC视图扩展类
    /// </summary>

    public static class ViewExtension
    {
        // public static T GetModel<T>(this IView self) where T : class, IModel, new() => self.Activity?.GetModel<T>();
        
        // public static T GetModel<T>(this IView self) where T : class, IModel, new()
        // {
        //     return (self as IController)?.GetModel<T>() ?? self.Activity?.GetModel<T>();
        // }
    }
}